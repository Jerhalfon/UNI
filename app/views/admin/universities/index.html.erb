<%= simple_form_for @university, method: :get, url: new_admin_university_program_path(@university) do |f| %>
  <%= f.input :name, collection: @universities %>
  <%= f.submit "Search", value: "search" %>
<% end %>

<%= link_to "create", new_admin_university_path  %>
